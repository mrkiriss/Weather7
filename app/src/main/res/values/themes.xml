<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blu_gray_800</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/orange_200</item>
        <!-- Customize your theme here. -->
        <item name="colorOnSurface">@color/blu_gray_600</item>
        <item name="android:textColor">@color/blu_gray_50</item>
        <item name="android:editTextColor">@color/light_green_800</item>
    </style>
</resources>